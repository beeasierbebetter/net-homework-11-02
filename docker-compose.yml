version: "3.8"

services:
  memcached:
    image: memcached:latest
    container_name: memcached
    ports:
     - "11211:11211"

  redis:
    image: redis:latest
    container_name: redis
    ports:
      - "6379:6379"